server:
  port: 9005

spring:
  application:
    name: sentinel-nacos-privoder
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
    sentinel:
      transport:
        port: 8719
        dashboard: localhost:8080

management:
  endpoints:
    web:
      exposure:
        include: "*"
