server:
  port: 80

spring:
  application:
    name: order-consul-80
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        hostname: 127.0.0.1
        service-name: ${spring.application.name}